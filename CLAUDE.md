## SQL Style
- Commas at the start of lines (not end)
- `ELSE NULL` not `ELSE 0` in CASE expressions — never default to zero silently
- `SAFE_DIVIDE(x, y)` for all division — always ensure denominator is NULL-safe (not zero)
- Indent 2 spaces inside CTEs
- CTE names in `snake_case`, column aliases in `snake_case`
- Prioritize efficiency, but never at the cost of correctness or the code doing what is intended


# Claude Role
* You are a Business Analyst with a deep understanding of SQL and how to apply it to achieve strong analytical results
* You are to focus on accuracy even if that means disagreeing with me
* You should try and optimize for load efficiency but accuracy comes first - if you have to make a material trade off ask
* Always ask before pushing to github


# SQL Context
* BigQuery with actual UI is Metabase

# Database Context
* Always try and add a date filter because the data is typically partitioned on this data even if the column name is not exactly date
* When in doubt, wrap timestamp/datetime columns with `DATE()` — always cast to date rather than assume the type


## Table: `weward-1548152103232.silver.customer_data`
| Column | Type | Example Values | Description |
|--------|------|----------------|-------------|
| `customer_id` | integer (PK) | `17571120`, `26810073` | Primary key — unique user identifier |
| `adjust_id` | string (hash) | `477ff75dc9001b84...` | Adjust attribution SDK identifier |
| `signup_at` | datetime | `10/4/2023, 09:19` | User registration timestamp |
| `country` | string (ISO 2) | `IT`, `ZA` | Country code of the user |
| `lang` | string (ISO 2) | `IT`, `FR` | Language preference |
| `last_timezone` | string (tz) | `Africa/Abidjan` | Last known timezone (IANA format) |
| `app_version` | string (semver) | `7.5.20`, `8.8.0` | App version at last session |
| `platform` | string (enum) | `android`, `ios` | Device platform |
| `gender` | string (enum) | `male`, _(nullable)_ | User gender — often null |
| `birth_date` | date | `7/19/89`, _(nullable)_ | User date of birth — often null |
| `onboarding_goals` | string | _(nullable)_ | Goals captured during onboarding |
| `onboarding_referral_source` | string | _(nullable)_ | Referral source captured at onboarding |
| `ward_balance` | integer | `80`, `682` | Current Wards (in-app currency) balance |
| `revenue_eur` | float | `0.0068`, `0.14` | Revenue generated by user (EUR) |
| `real_cost_eur` | float | `0`, `0` | Actual cost attributed to user (EUR) |
| `hyp_cost_eur` | float | `-0.013`, `-0.13` | Hypothetical/modeled cost (EUR) |
| `profitability` | float | `-0.0065`, `0.0097` | Net profitability per user (EUR) |

### Notes
- This is our internal customer table — a row exists only for users who completed account creation
- `customer_id` is the primary key; `adjust_id` is a secondary attribution identifier from the Adjust SDK
- Costs (`real_cost_eur`, `hyp_cost_eur`) are stored as negative values; `profitability = revenue_eur + real_cost_eur + hyp_cost_eur`
- `gender`, `birth_date`, `onboarding_goals`, and `onboarding_referral_source` are sparse/nullable fields
- All monetary columns are denominated in EUR
- `ward_balance` reflects WeWard points economy balance, not direct cash value


## Table: `weward-1548152103232.silver.install_adjust`
| Column | Type | Example Values | Description |
|--------|------|----------------|-------------|
| `adjust_id` | string (hash) | `09217bcc55809501...` | Primary key — Adjust attribution SDK identifier |
| `installed_at` | datetime | `1/16/2025, 01:08` | App install timestamp |
| `adjust_country` | string (ISO 2) | `AT`, `AU`, _(nullable)_ | Country detected by Adjust at install — often null |
| `adjust_platform` | string (enum) | `ios`, `android` | Device platform at install |
| `network` | string | `TikTok SAN`, `Google Organic Search`, `Organic`, `referral` | Attribution network source |
| `adgroup` | string | _(nullable)_ | Ad group within the campaign — often null |
| `campaign` | string | `weward app`, _(nullable)_ | Campaign name — often null |
| `creative` | string | _(nullable)_ | Creative asset identifier — often null |

### Notes
- This is data from the Adjust SDK and includes all installs — both users who created an account and those who did not
- `adjust_id` is the primary key and joins to `weward-1548152103232.silver.customer_data` on `adjust_id`; a NULL match means the installer never registered
- `adjust_country` is nullable — can be missing even when other fields are populated
- `adgroup`, `campaign`, and `creative` are sparse; only populated for paid/tracked campaigns
- `network` values include both paid channels (e.g. `TikTok SAN`) and organic sources (e.g. `Organic`, `referral`)
- This table represents one row per install event, not per user session


## Table: `weward-1548152103232.silver.campaign_adjust`
> Campaign-level performance data aggregated from Adjust, covering spend, delivery metrics, and install activity across paid marketing channels and strategies.
| Column | Type | Example Values | Description |
|--------|------|----------------|-------------|
| `date` | date | `2/23/26` | Reporting date for the aggregated metrics |
| `channel` | string | `Applovin`, `Adjoe` | High-level marketing channel |
| `network` | string | `Applovin`, `Adjoe-offerwall` | Specific ad network within the channel |
| `adgroup` | string | `unknown`, `fbc9047c-30df...` | Ad group identifier — often `unknown` for some networks |
| `campaign` | string | `Weward_iOS_CPE_TRUE_USERS_CPM (97ed10...)` | Full campaign name including hash identifier |
| `creative` | string | `0`, `08c7df0a-429f...` | Creative asset ID — `0` when not applicable |
| `adjust_country` | string (ISO 2) | `DE`, `US` | Country targeted by the campaign |
| `adjust_platform` | string (enum) | `ios`, `android` | Platform targeted by the campaign |
| `adjust_app_version` | string | `unknown` | App version targeted — often `unknown` |
| `network_cost` | float | `0.17`, `4.6`, `4.06` | Spend reported by the network (EUR assumed) |
| `impressions` | integer | `0` | Number of ad impressions delivered |
| `installs` | integer | `0` | Installs attributed by Adjust |
| `skad_total_installs` | integer | `0` | Installs attributed via SKAdNetwork (iOS privacy framework) |
| `clicks` | integer | `0` | Number of ad clicks |
| `reinstalls` | integer | `0` | Number of reinstalls attributed |
| `ecpi` | float | `0` | Effective cost per install — derived metric |

### Notes
- Primary grain: one row per `date` + `channel` + `network` + `adgroup` + `campaign` + `creative` + `adjust_country` + `adjust_platform` combination
- `network_cost` is the key spend metric; denominated in EUR
- `ecpi` is a derived field: `network_cost / installs` — will be `0` or null when installs are 0
- `skad_total_installs` is iOS-specific; reflects installs reported under Apple's privacy-preserving attribution framework
- `adgroup` and `creative` may contain UUIDs (Adjoe) or be `unknown` (Applovin) depending on network reporting capabilities
- Join to `weward-1548152103232.silver.install_adjust` via `network`, `campaign`, `adgroup`, `creative` for install-level attribution detail


## Table: `weward-1548152103232.silver.customer_revenue`
> Customer-level daily revenue data, broken down by revenue type (campaign_category) and source. Used to understand how and where revenue is generated per user across different monetization strategies.
| Column | Type | Example Values | Description |
|--------|------|----------------|-------------|
| `customer_id` | integer (FK) | `26311048`, `14560278` | User identifier — foreign key to `customer_data.customer_id` |
| `date` | date | `1/1/26` | Date the revenue was recorded |
| `campaign_category` | string (enum) | `freeoffer` | Type/category of revenue (e.g. free offers, ads, subscriptions) |
| `source` | string | `Altroconsumo_2025_12_01_IT` | Specific revenue source or campaign driving the revenue |
| `revenue_eur` | float | `0.58` | Revenue amount in EUR for that user on that date |

### Notes
- Primary grain: one row per `customer_id` + `date` + `campaign_category` + `source` combination
- `customer_id` is the join key to `weward-1548152103232.silver.customer_data`
- `campaign_category` is the top-level revenue classification — expect multiple enum values beyond `freeoffer` (e.g. ads, subscriptions, partnerships)
- `source` follows a naming convention of `{partner}_{YYYY}_{MM}_{DD}_{country}` — useful for slicing revenue by partner and market
- Aggregate `SUM(revenue_eur)` grouped by `customer_id` to reconcile with `customer_data.revenue_eur`
- Multiple customers can share the same `source` and `revenue_eur` value on the same date (flat-rate offer payouts)


## Table: `weward-1548152103232.silver.customer_cost`
> Customer-level daily ward (in-app currency) transaction log. Tracks both earning events (positive) and redemption/removal events (negative) per user, broken down by activity type.
| Column | Type | Example Values | Description |
|--------|------|----------------|-------------|
| `customer_id` | integer (FK) | `28506128`, `28505838` | User identifier — foreign key to `customer_data.customer_id` |
| `date` | date | `1/1/26` | Date the ward transaction occurred |
| `campaign_category` | string (enum) | `bonus`, `challenge_v2` | Type of ward earning or redemption activity |
| `amount_ward` | integer | `150`, `25`, `0`, `-500` | Ward amount — positive = earned, negative = redeemed or removed |

### Notes
- Primary grain: one row per `customer_id` + `date` + `campaign_category` combination
- `customer_id` is the join key to `weward-1548152103232.silver.customer_data`
- `amount_ward` sign convention:
  - **Positive** — user earned wards (e.g. via `bonus`, `challenge_v2`, walking, offers, etc.)
  - **Negative** — wards left the user's balance via one of three redemption/removal categories:
    - `admin` — wards forcibly removed due to abuse/fraud
    - `bank_transfer` — user redeemed wards for cash payout
    - `redeem` — user redeemed wards for a gift card
- `SUM(amount_ward)` grouped by `customer_id` should reconcile with `customer_data.ward_balance`
- Zero-value rows (e.g. `challenge_v2` with `0`) represent participation events with no ward outcome


## Table: `weward-1548152103232.silver.customer_activity`
> Daily active user log. A customer's presence in this table on a given date indicates they were active that day. The `state` column classifies each user's engagement status at the time of activity.
| Column | Type | Example Values | Description |
|--------|------|----------------|-------------|
| `customer_id` | integer (FK) | `1249571`, `3881822` | User identifier — foreign key to `customer_data.customer_id` |
| `country` | string (ISO 2) | `BE` | Country of the user on that date |
| `date` | date | `1/1/26` | Date the user was active |
| `state` | string (enum) | `CURRENT`, `REACTIVATED`, `RESURRECTED`, `NEW_USER` | User engagement state on that active day |
| `step_amount` | integer | _(nullable)_ | Raw step count recorded for the day |
| `step_amount_validated` | integer | _(nullable)_ | Validated step count after fraud/anomaly filtering |

### Notes
- Primary grain: one row per `customer_id` + `date` — presence in this table = active that day
- `customer_id` is the join key to `weward-1548152103232.silver.customer_data`
- `state` enum definitions:
  - `NEW_USER` — first active day since install
  - `CURRENT` — active user who was also active in the recent prior period
  - `REACTIVATED` — user who was inactive for a defined period and has returned
  - `RESURRECTED` — user who was inactive for a longer/distinct period and has returned
- `step_amount` and `step_amount_validated` are nullable — not all activity types require step data
- Use this table as the basis for DAU, MAU, retention, and reactivation metrics
- `country` here may differ from `customer_data.country` if the user has traveled or changed region


## Table: `696845466639.costs.fct_cashout`
> Transaction-level log of all ward spend events. A record here means a user spent (redeemed) wards — whether for charity donations, cash withdrawals, virtual goods, or community transfers. Negative values reflect outflows from both the user's ward balance and WeWard's cost base.
| Column | Type | Example Values | Description |
|--------|------|----------------|-------------|
| `created_at` | date | `8/7/25` | Date the transaction occurred |
| `customer_id` | integer (FK) | `25039315`, `6368781` | User identifier — foreign key to `customer_data.customer_id` |
| `product_id` | string | `care_venus_25`, `savethedogs_june25`, `xp_double_24`, `other` | Specific product or campaign the wards were spent on |
| `transaction_id` | integer | `5486607194` | Unique transaction identifier |
| `amount` | float | `-256`, `-1.82`, `-550` | Ward or unit amount spent — always negative (outflow from user) |
| `product_type` | string (enum) | `fundraising`, `community_transfer`, `app_item` | Category of the spend event |
| `ward_value` | float | `0.0025`, `0` | EUR value per ward unit — derived as `cost_eur / amount` (negatives cancel); `0` when no monetary cost |
| `cost_eur` | float | `-0.64`, `-1.38`, `0` | Cost to WeWard in EUR — negative means real monetary outflow; `0` for non-cash transactions |

### Notes
- Primary grain: one row per `transaction_id` — unique per spend event
- `customer_id` is the join key to `customer_data.customer_id`
- `amount` is always negative — represents ward outflow from the user's balance
- `cost_eur` sign convention: negative = real cost to WeWard (cash left the business); `0` = no monetary impact (e.g. `community_transfer`, `app_item`)
- `ward_value` = `cost_eur / amount` — negatives cancel, yielding a positive EUR-per-ward rate; `0` when transaction has no monetary value
- `product_type` enum observed values:
  - `fundraising` — user donated wards to a charity campaign (has monetary cost)
  - `community_transfer` — ward transfer between users or community pools (no monetary cost)
  - `app_item` — virtual good purchased within the app (no monetary cost)
- `product_id` of `other` is a catch-all for unclassified or non-specific products
- Join to `weward-1548152103232.silver.customer_wards` on `customer_id` + `created_at` to reconcile ward balance movements


## Table: `weward-1548152103232.costs.fct_cashout_daily_activity_lifetime`
> Lifetime cumulative cashout totals per user, broken down by time since install and product type. Each row represents a user's total accumulated cashout spend up to a given day/week/month since their install — not a point-in-time daily event log.
| Column | Type | Example Values | Description |
|--------|------|----------------|-------------|
| `created_at` | date | `7/29/24` | Date of the cashout event |
| `customer_id` | integer (FK) | `77`, `961`, `2682` | User identifier — foreign key to `customer_data.customer_id` |
| `product_type` | string (enum) | `app_item`, `fundraising`, `community_transfer` | Category of the spend event |
| `day_after_install` | integer | `1846` | Days since install when this cashout occurred |
| `week_after_install` | integer | `263` | Weeks since install when this cashout occurred |
| `month_after_install` | integer | `60` | Months since install when this cashout occurred |
| `amount` | integer | `-10550`, `-50`, `-1000` | Cumulative wards spent up to this point — always negative |
| `cost_eur` | float | `0`, `-0.64` | Cumulative cost to WeWard in EUR up to this point — negative = real monetary outflow |

### Notes
- Primary grain: one row per `created_at` + `customer_id` + `product_type`
- `amount` and `cost_eur` are **lifetime cumulative totals** as of each `day_after_install` — do not sum across rows for a single user as this will double-count
- To get a user's total lifetime cashout, take their maximum `day_after_install` row (or filter to a specific day window)
- Use `day_after_install` / `week_after_install` / `month_after_install` to compare cohorts at equivalent points in their lifecycle
- Use `fct_cashout` (transaction-level) when you need non-cumulative daily cashout amounts


## Table: `696845466639.raw_data.store_order`
> Transaction-level log of all redemption requests submitted by users. Each row represents a single redemption order (cash transfers, gift cards, donations, app items, etc.), tracking its status and outcome. The EUR value of the redemption must be parsed from `product_id` — `price` is in wards only.
| Column | Type | Example Values | Description |
|--------|------|----------------|-------------|
| `id` | integer (PK) | `1725369`, `1750454` | Unique order identifier |
| `customer_id` | integer (FK, nullable) | `77`, _(nullable)_ | User identifier — foreign key to `customer_data.customer_id`; nullable for some declined orders |
| `product_id` | string | `bancario_15_eur`, `virement_20_eur`, `bancario_60_eur` | Product redeemed — follows `{payment_method}_{amount}_{currency}` naming convention |
| `price` | integer | `3000`, `4400`, `12000` | Ward cost of the redemption — in wards, not EUR |
| `status` | string (enum) | `delivered`, `declined`, `pending`, `failed`, `refunded` | Outcome of the redemption request |
| `created_at` | datetime | `6/17/2022, 20:32` | Timestamp when the order was submitted |
| `updated_at` | datetime | `6/21/2022, 04:44` | Timestamp of last status update |
| `delivery_date` | datetime (nullable) | `6/23/2022, 12:14`, _(nullable)_ | Timestamp when order was fulfilled — only populated for `delivered` orders |
| `transaction_id` | integer (nullable) | `1,980,110,355`, _(nullable)_ | Payment transaction reference — only populated for `delivered` orders |
| `transfer_id` | integer (nullable) | `913581`, _(nullable)_ | Transfer reference identifier — sparse |
| `wardeal_id` | integer (nullable) | `77`, _(nullable)_ | Ward deal reference — sparse |
| `actor` | integer | `246` | Internal actor/agent identifier — likely system or admin user processing the order |
| `declined_reason` | string (nullable) | `user_is_cheating_ban`, _(nullable)_ | Reason for decline — only populated for `declined` orders |
| `_airbyte_raw_id` | string (UUID) | `2573819c-18e5-4ff4-...` | Airbyte internal tracking ID — ignore for analysis |
| `_airbyte_extracted_at` | datetime | `3/5/2026, 05:46` | Airbyte extraction timestamp — ignore for analysis |
| `_airbyte_meta` | string (JSON) | `{"changes":[],"sync_id":6446}` | Airbyte sync metadata — ignore for analysis |
| `_airbyte_generation_id` | integer | `246` | Airbyte generation identifier — ignore for analysis |

### Notes
- Primary grain: one row per `id` — unique per cashout order
- `customer_id` is the join key to `weward-1548152103232.silver.customer_data`; filter out NULLs when joining
- `status` enum: `delivered` = successfully fulfilled; `declined` = rejected before processing; `pending` = awaiting processing; `failed` = processing attempted but failed; `refunded` = fulfilled then reversed
- `delivery_date` and `transaction_id` are only populated when `status = 'delivered'`
- `declined_reason` is only populated when `status = 'declined'`; `user_is_cheating_ban` indicates fraud/abuse
- `product_id` naming convention: `{payment_method}_{eur_amount}_eur` — e.g. `bancario_15_eur` = bank transfer of €15, `virement_20_eur` = wire transfer of €20
- **To get EUR value: parse the numeric portion from `product_id`** using `REGEXP_EXTRACT(product_id, r'_(\d+)_eur')` — this is the only source of EUR redemption value in this table
- `price` is in wards only — it is NOT the EUR value
- Not all `product_id` values follow the EUR pattern (e.g. non-cash redemptions may differ) — always inspect before assuming EUR can be extracted
- Ignore all `_airbyte_*` columns — they are pipeline metadata, not business data
